{
  "name": "docket",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Knowledge article approval workflow UI (docket).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node test/diff.test.mjs && node test/resolve.test.mjs"
  },
  "dependencies": {
    "lucide": "^0.400.0"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vite-plugin-singlefile": "^2.0.0"
  }
}
